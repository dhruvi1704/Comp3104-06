{
  "name": "your-project-name",
  "version": "1.0.0",
  "homepage": "https://<your-username>.github.io/<repository-name>",
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"s
  }
}
